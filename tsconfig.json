{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "paths": {
      "@/*": ["./*"]
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts"
  ],
  "// exclude note": "scripts/ are Node tooling (rls-test.ts), run with tsx against Supabase — not part of the RN bundle. They need @types/node, which the app deliberately does not, so they are typechecked in that environment, not here.",
  "exclude": ["node_modules", "scripts"]
}
